DATABASE_URL="postgresql://user:password@localhost:5432/trading_platform?schema=public"

# Generate a strong random value, e.g. `openssl rand -base64 32`
AUTH_SECRET="replace-with-a-long-random-secret"

# The account that registers with this email is auto-promoted to ADMIN + APPROVED.
ADMIN_EMAIL="you@example.com"

# Anthropic API key powering the AI strategy assistant chatbot.
ANTHROPIC_API_KEY="sk-ant-..."
